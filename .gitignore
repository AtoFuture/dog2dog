# ---- 交付/分发压缩包，不入库（内容已解压到 创新实践/）----
*.zip

# ---- 编辑器 / 系统 ----
.DS_Store

# ---- 媒体素材，不入库 ----
# 测试用视频/图片素材体积大且与代码无关。
# 需要时从原处取，不要提交进仓库（会永久留在历史里，clone 越来越慢）。
*.mp4
*.avi
*.mov
*.mkv

# ---- 数据集 ----
# 实测数据放在仓库外（~/.g2_testdata 之类），别往仓库里拷。
*.npy
