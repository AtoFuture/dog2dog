# P1 可信运动与建图闭环 · 交接

> **交接日期**：2026-09-23
> **对应计划**：`docs/项目验证修复与智能大脑集成计划.md` 的 **P1**
> **代码基线**：`develop` @ `2f23a3b`（已推送）
> **详细工具说明**：`baseline/g1_mapping_nav/tools/README.md`
> **证据 rosbag**：`/home/wy/g1_bags/p1_20260923/`（374 MB）

---

## 一句话状态

**出口标准 4 项里 3 项已验证通过，第 5 项没做。** 「到点误差」这条从只能看
action 的 `SUCCEEDED`，变成了能用真值判定，并且 5 个目标点全部达标。
剩下的缺口是「导航期间保持 SLAM 运行、证明地图能边走边更新」——本轮验收用的是
**AMCL 加载静态地图**，不是边跑边建图。

---

## 一、已验证通过的部分

环境：隔离域 `ROS_DOMAIN_ID=43` + `IGN_PARTITION=wy`，无头仿真，全程按**真值**判定。

| 出口标准 | 结果 |
|---|---|
| 静止 120 s 虚假位移 ≤ 0.02 m | ✅ **0.0000 m** |
| ground truth 到点误差 ≤ 0.30 m，成功率 ≥ 4/5 | ✅ **5/5**（0.121 / 0.105 / 0.111 / 0.115 / 0.269 m） |
| 不出现重复控制器、留存速度指令 | ✅ 单例正常，且启动脚本已加自检 |
| rosbag 含地图、TF、Nav2 goal/result、真值 | ✅ 四要素齐全 |

五轮全程 z 稳定在 0.25~0.28，**没有一次摔倒**。

### 一个值得看的对比

AMCL 参数收紧前后（`config/go2_nav2.yaml`）：

| | 收紧前 | 收紧后 |
|---|---|---|
| AMCL 位姿更新数（~75 s） | 18 / 35 | 72 / 118 / 65 / 181 |
| AMCL 对真值最大偏差 | 0.464 m | ≤ 0.224 m |
| 到点误差 | 0.362 / 0.028 / 0.202 / 0.232（4/5） | 全部 ≤0.269（5/5） |

---

## 二、接手前必须补的三处改动

**这三处不在本仓库里。** 拿一台新机器从 `develop` 复现，缺了它们跑不起来：

| 位置 | 改什么 | 为什么 |
|---|---|---|
| `ROS2-Gazebo-GO2/src/gazebo_sim/launch/launch.py` | 加受 `GZ_HEADLESS` 控制的 `-s`，并在无头时转发子 launch 的 `enable_rviz` 开关 | 原文件把 `gz_args` 写死 `-r -v4`，没有无头入口；不关 rviz2 的话它在无 DISPLAY 下 abort，会带崩整个 launch |
| `~/go2_sim_ws/run_sim_iso.sh` | 加 `GZ_HEADLESS` 传递 + EGL vendor 修复 | 见下 |
| `~/go2_sim_ws/g1ros.sh` | 新建（隔离域里跑 ROS 命令的入口） | 隔离三元组里 `ROS_DOMAIN_ID` / `IGN_PARTITION` 必须在**容器内**设置，漏设会静默连到别人的仿真 |

⚠️ **`~/go2_sim_ws/` 下的脚本不属于任何 git 仓库**，换机器就丢，建议尽快纳入版本管理。

### EGL 那个坑值得单独说

无头起仿真后传感器渲染线程刷屏 `libEGL warning: egl: failed to create dri2 screen`，
然后 SIGSEGV。根因不是配置，是 **enroot 只注入 NVIDIA 的 `.so`、不注入 glvnd 的
EGL vendor 配置**——容器里 `/usr/share/glvnd/egl_vendor.d/` 只有 `50_mesa.json`，
于是 EGL 被派发给 Mesa 走 DRI2（那是需要 X 的路径）。

解法不动容器镜像：把 `10_nvidia.json` 放进已挂载的 `/tmp`，
用 `__EGL_VENDOR_LIBRARY_FILENAMES` 指过去。
另外**必须用 `~/.enroot/ros-gui` 容器拿 GPU 库**，纯 `ros` 容器里根本没有
NVIDIA 图形库（实测少了 libnvidia-glcore 等 41 个库）。

---

## 三、标准流程

```bash
cd ~/go2_sim_ws

# 1) 起隔离无头仿真（约 1 分钟）
#    GUI=1 是为了要 ros-gui 容器的 GPU 库，GZ_HEADLESS=1 才不画窗口
GUI=1 GZ_HEADLESS=1 ROS_DOMAIN_ID=43 IGN_PARTITION=wy ./run_sim_iso.sh &

# 2) 控制器激活自检 + 速度缩放器 + Nav2
./g1ros.sh bash <repo>/baseline/g1_mapping_nav/tools/start_nav2_isolated.sh

# 3) 从真值设 AMCL 初始位姿（配置里没有 set_initial_pose，必须外部发）
./g1ros.sh bash <repo>/baseline/g1_mapping_nav/tools/set_initial_pose.sh

# 4) 发目标 + 录包 + 真值判定
./g1ros.sh bash <repo>/baseline/g1_mapping_nav/tools/goal_test.sh 10.5 3.0 0.0 80 /tmp/out
```

**时间预算**：仿真实时率约 **0.72**——120 s 的仿真时间要留 167 s 挂钟。

---

## 四、三个会静默污染结论的坑

### 1. `joint_group_controller` 会停在 `unconfigured`，狗一动不动

现象极具迷惑性：**仿真看着全好**——`/clock` 在走、节点齐全、
`commands` 话题发布者数 = 1、`quadruped_controller` 以 29 Hz 照发命令，
但真值净位移 0.0000 m、z 恒在生成高度 0.446 而非站立 0.27。

根因是控制器没被激活（launch 里的 spawner 有竞态）。`configure_controller` 可能返回
`ok=False`，但 `switch_controller` 能激活。`start_nav2_isolated.sh` 第 1 步会自检并停下。

### 2. action 的 `SUCCEEDED` 不能当到达判据

实测有一轮 action 报 `SUCCEEDED` 而真值在 **0.362 m** 外。
因为 **goal checker 判的是 AMCL 的位姿，不是真值**，而当时 AMCL 对真值偏了 0.464 m。

根因是 AMCL 更新太稀：`update_min_d: 0.25` / `update_min_a: 0.2` 意味着要动 25 cm
或转 0.2 rad 才更新一次，实测约 0.3 Hz，两次更新之间 goal checker 拿的是陈旧估计。
`transform_tolerance: 10.0` 还允许超时使用 10 秒前的变换。三个值都已收紧。

**反过来也要警惕**：后来有一轮 G5 物理上到了（0.115 m）但 action 在 80 s 内没报成功——
「报成功没到」和「到了没报成功」都会出现，**两个方向都要核**。

### 3. 速度指令被放大 ~10 倍

`cmd_vel_pub.py` 里 `multiply_and_limit` 的 scale 被从上游的 `0.035` 改成了 `0.3`
（**这是仿真工作区里一处我们没动过、也没提交的改动**），而步态对 rv 的增益本身约 10 倍。
合起来 Nav2 按配置发 0.26 m/s，实际会跑出 ~2.7 m/s、rv 顶到 0.27，
远超实测稳定边界 **rv < 0.04**（cmd 0.06 档就原地蹬腿不前进，路径 1.9 m 净位移 0.03 m）。

`tools/cmd_vel_scaler.py` 的系数 0.115 与上游 0.035 相乘 ≈ 0.0345，
**本质上是把我们这一路恢复到上游默认值**。缩放只落在我们这份配置里，共享代码未动。

---

## 五、未完成 / 已知问题

| 项 | 状态 |
|---|---|
| **P1 第 5 项：导航期间保持 SLAM 运行** | ❌ **没做**。本轮是 AMCL + 静态地图，不是边跑边建图 |
| G5 的 action 未收敛 | ⚠️ 物理到达 0.115 m，但 action 80 s 内没报成功，**原因未查**（疑似终点朝向容差） |
| 足端里程计本身的修复 | ⚠️ 只做了**隔离**（判据改用真值），**没修算法**。离线复现器 `tools/odom_replay.py` 已就绪，可直接迭代 |
| odom 幻觉的量级 | 已量化但**未修**：正常行走误差约 10%；狗原地蹬腿时虚报可达 **83 倍**（真值 0.028 m vs odom 2.32 m） |
| costmap 是否异常 | ❓ 未单独量 |
| 效率 | ⚠️ 路径长偏大：1.10 m 直线走了 3.86 m（绕 3.5 倍） |

### 已经查清、**不需要**再查的

- 「AMCL 从不收敛」的真因不是 AMCL 参数，是**没给初始位姿**（配置里没有
  `set_initial_pose`）。历史两份 nav2.log 里 79 次 / 329 次报错都是这个。
- 地图与世界系**恒等对齐**（地图原点 = Gazebo 世界原点），实测 AMCL 误差 0.150 m。
  所以量到点误差可以直接把真值坐标当 map 坐标用。
- 「DWB 零速」在日志里**没有证据**，那是文档里的归因，与实测不符。

---

## 六、资产位置

| 资产 | 位置 |
|---|---|
| 工具与详细说明 | `baseline/g1_mapping_nav/tools/`（10 个工具 + README） |
| Nav2 参数（含 AMCL 收紧） | `baseline/g1_mapping_nav/config/go2_nav2.yaml` |
| 证据 rosbag | `/home/wy/g1_bags/p1_20260923/` |
| 仿真侧改动 | `~/go2_sim_ws/`（**不在仓库**，见第二节） |

证据包里值得看的：

- `amcl_G1..G5/` —— 收紧 AMCL 后 5 个目标点的成功包（含真值、TF、地图、action）
- `bag_static120/` —— 静止 120 s 零漂移的原始数据
- `bag_replay/` —— 狗趴着蹬腿、odom 虚报 83 倍的那一轮

---

## 七、建议的下一步顺序

1. **补齐 P1 第 5 项**——起 `slam_toolbox` 替掉 AMCL，验证地图随行进增长。
   这是 P1 唯一的硬缺口。
2. **查 G5 的 action 未收敛**——「到了没报成功」会掩盖真实失败。
3. **修足端里程计**——`odom_replay.py` 已能离线复现，改算法不必反复跑仿真。
   注意：正常行走时它误差只有 10%，真正崩的是「原地蹬腿」这类异常态。
4. 效率问题可以放到 P2 一起看（连续 frontier 探索更在意路径质量）。
