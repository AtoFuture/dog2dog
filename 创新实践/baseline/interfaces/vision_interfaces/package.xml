<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>vision_interfaces</name>
  <version>0.1.0</version>
  <description>
    G2 视觉探索的对外接口消息。目前只含接口 03：Detection3D。
    放在 baseline/interfaces/ 下而非 G2 目录内 —— 它是 G2 与 G3 的共用契约，
    按仓库规矩各组「不跨目录改别人的文件」，放在谁的项目里都是给谁添负担。
  </description>

  <maintainer email="240923267+AtoFuture@users.noreply.github.com">AtoFuture</maintainer>
  <license>Apache-2.0</license>

  <buildtool_depend>ament_cmake</buildtool_depend>
  <buildtool_depend>rosidl_default_generators</buildtool_depend>

  <exec_depend>rosidl_default_runtime</exec_depend>
  <member_of_group>rosidl_interface_packages</member_of_group>

  <!-- 消息里嵌套了这些包的类型。缺一个就会编译失败，
       且 rosidl 的报错信息很不直观（通常只说找不到符号）。 -->
  <depend>std_msgs</depend>
  <depend>geometry_msgs</depend>
  <depend>sensor_msgs</depend>
  <depend>builtin_interfaces</depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
