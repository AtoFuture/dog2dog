# 一次目标检测的结果。
#
# 接口 03（G2 视觉探索 -> G3 集成评测）
# 契约来源：baseline/README.md 的接口表
#
# 语义冻结（按契约硬规矩 2：改语义必须全队通知，加字段可随时）
#   * pose 是 **map 系** 下的三维坐标 —— 契约点名的红线。
#     要改成相机系或机体系必须全队通知，否则三组一起挂。
#   * stamp 是 **图像采集时刻**，不是处理完成时刻。
#     用处理时刻会让三维坐标与当时的 TF 对不上，尤其在回传时延大的链路上。
#
# 本消息**不含抓拍图**。图走独立话题，见 DetectionSnapshot.msg。

string class_name        # 类别名（COCO 类名，或异常类 person_fallen / smoke）

float32 confidence       # 置信度 0~1

geometry_msgs/PoseWithCovariance pose
# 三维坐标，map 系。
#
# position    目标代表点。
#             代表点的定义（bbox 中心 / 底部中点）由 G2 侧参数控制，
#             并在 Detection3D 之外的文档中约定 —— 这个语义一变，
#             G3 的评测口径会跟着变，比坐标系变化更隐蔽。
#
#             ⚠️ **异常类例外**（2026-09-18 写进契约，审核 P1-②）：
#             source != "coco" 时代表点是**躯干中点**（肩中点与髋中点的平均），
#             **不受 anchor 参数控制**。躺姿下的躯干中点与 bbox 底部中点
#             差得很远，G3 若按同一套口径做关联/画图会系统性偏移。
#
#             ⚠️ 另外：同一帧里同一个 id 可能产生**两条**消息
#             （coco + pose），stamp 都是该帧的图像采集时刻 ——
#             所以 (id, stamp) 不是唯一键，要加上 source。
#
# covariance  **一律置零**（2026-09-17 修正）。
#             按 ROS 惯例，全零表示「协方差未知」——这是诚实的。
#
#             修订说明：初版说这里填「三维解算时像素的离散程度」。那是错的 ——
#             从检测框反投影得到的像素离散衡量的是**物体自身的空间展布**
#             （一辆 4 m 长的车，深度方向 IQR 有 1.5 m），而不是测量误差
#             （可能是 5 cm 量级）。把它当协方差填进来会让 G3 的
#             数据关联与加权完全失真。
#
#             G2 目前**无法**给出有意义的测量协方差。真要给，需要的是
#             深度传感器噪声模型 + 外参标定残差 + 时间同步误差的合成，
#             那是有价值的后续工作，但现在是零，不是编一个数。
#
#             ⚠️ G3 请注意：**不要**假设这个字段有信息量。
#
# orientation **不使用**，恒为单位四元数 (0,0,0,1)。
#             G3 不应对它做任何假设 —— 检测结果本质是一个点，没有朝向。

builtin_interfaces/Time stamp   # 图像采集时刻

uint32 id                # 跟踪 ID（去重用）。
                         # 同一目标被连续检出时 id 不变。G3 据此可区分
                         # 「同一目标的多次上报」与「多个不同目标」——
                         # 没有它，检出率/误检率的统计只能依赖
                         # "上游发了几条"这种脆弱假设。
                         #
                         # ⚠️ **0 是保留值，表示「本次检测没有跟踪 ID」**
                         # （跟踪器还没确认这条轨迹时会给不出 id）。
                         # G3 **不要**把 id=0 的消息当成同一个目标去合并 ——
                         # 那会把画面里所有未确认的目标错误地并成一个。
                         # 对 id=0 的处理方式是：各自独立计数，不做去重。

string source            # 来源：coco | pose | heuristic
                         # 让 G3 能**确定性过滤**，不必靠 class_name 字符串约定去猜。
                         # 例如「异常类不计入误检率」直接判 source != "coco" 即可。

bool has_snapshot        # 本检测在 /detections_snapshot 上是否有对应抓拍图。
                         # G3 做指标统计时不必订阅图像话题；只在需要看图时订阅，
                         # 并靠 (id, stamp) 匹配。
                         #
                         # ⚠️ (id, stamp) 不是唯一键 —— 同一帧里同一个 id 可能
                         # 产生两条消息（coco + pose）。严格来说要加上 source。
                         # 见上面 pose 那段。


# ============================================================================
# 以下三个字段是 2026-09-18 新增的（**加字段**，未改任何既有语义）。
#
# 背景：倒地判据从「单帧躯干倾角」改成了「时间上的状态转移」。
# 实测证明单帧判据不成立 —— 跪、蹲、弯腰的躯干同样不水平，
# 一个四肢着地跪趴的人测出 84°，比最轻的真倒地（75°）还高。
# 详见 g2_vision_explore/docs/倒地判据实测-方法B.md。
#
# 判据现在依赖**同一个 id 的跨帧观测序列**，于是有三件事必须说清。
# ============================================================================

builtin_interfaces/Time onset_stamp
# **异常事件的起始时刻**（首次观测到目标倒下的时刻），不是确认时刻，
# 也不是本消息的发送时刻。
#
# 为什么不能用 stamp 代替：异常类是**边沿触发**的 ——
# 一个目标一次事件只发**一条**消息，而不是躺着就一直逐帧重发。
# 于是「躺着不动」这段时间里没有任何消息，G3 若拿 stamp 当时刻统计，
# 会把「一次倒地」算成「零次」。
#
# 非异常类（source == "coco"）填 0。
#
# ⚠️ G3 统计倒地**次数**时用 onset_stamp 去重，用 stamp 会重复计数。

float32 torso_tilt_deg
# 三维躯干与重力方向的夹角（度）。**原始观测量，不是判定结果。**
# 0 = 完全直立，90 = 完全水平。NaN = 本帧没测出来。
#
# 为什么把中间量也发出来：G2 的判定阈值是在有限数据上标的（实测样本
# 只有十几条轨迹），很可能需要重标。把原始量发出来，G3 就能在**已经录好的
# rosbag 上重扫阈值**，不必重跑感知、不必重新采集。
# 这一条在本次实测里被证明很值钱 —— 正是因为留下了原始量，
# 才发现正负两类的倾角分布是重叠的。
#
# 非异常类填 NaN。

float32 torso_height_m
# **躯干中点离地高度**（米）—— 即**肩中点与髋中点的 z 平均**，
# 在 map 系下量（map 的 z=0 约定为地面）。NaN = 本帧没测出来。
# 
# ⚠️ **口径必须说死，因为文档里另有一组「髋部高度」的数字，两者差得不小**
# （2026-09-18 审核 P1-③ 发现：接口发的是躯干中点，而 `docs/倒地判据实测-方法B.md`
# 和 `fall_tracker.py` 里引用的标定数字全是**髋部**高度）。
# 将来有人按那些数字打开 `fall_max_height_m`，对的就是另一个量。
# 
# 目前**没有**针对「躯干中点」标定过的数字。
#
# 这个量能把「躺在地上」和「跪着 / 蹲着」分开 —— 那正是它存在的理由。
#
# ⚠️ **G2 目前还没标定它的阈值，判定时并未使用它** ——
# 实测里真倒地一侧出现了 1.58 m 这种不可能的值（深度测量错误）。
# 所以它现在是**仅供 G3 分析与后续标定**的观测量，不要拿它做判定。
#
# 非异常类填 NaN。
