# 一次目标检测的结果。
#
# 接口 03（G2 视觉探索 -> G3 集成评测）
# 契约来源：baseline/README.md 的接口表
#
# 语义冻结（按契约硬规矩 2：改语义必须全队通知，加字段可随时）
#   * pose 是 **map 系** 下的三维坐标 —— 这是契约点名的红线。
#     若哪天要改成相机系或机体系，必须全队通知，否则三组一起挂。
#   * stamp 是 **图像采集时刻**，不是处理完成时刻。
#     用处理时刻会让三维坐标与当时的 TF 对不上，尤其在回传时延大的链路上。

string class_name        # 类别名（COCO 类名，或异常类 person_fallen / smoke）

float32 confidence       # 置信度 0~1

geometry_msgs/Pose pose  # 三维坐标，map 系

builtin_interfaces/Time stamp   # 图像采集时刻

sensor_msgs/Image snapshot      # 抓拍图

# ---------------------------------------------------------------------------
# 以下为 G2 新增字段。契约允许「只加字段、不改语义」，故不违反冻结。
# ---------------------------------------------------------------------------

uint32 id                # 跟踪 ID。
                         # 用途：去重。同一个人被连续检出时 id 不变，
                         # G3 据此可区分「同一个目标的多次上报」与「多个目标」。
                         # 没有这个字段，检出率/误检率的统计口径会依赖
                         # "上游发了多少条"这种脆弱的假设。

string source            # 来源：coco | pose | heuristic
                         # 用途：让 G3 能**确定性地过滤**，而不是靠 class_name
                         # 字符串约定去猜。
                         # 例如「smoke 用启发式产生、不计入误检率」这种规则，
                         # 直接判 source != "coco" 即可，不必枚举类别名。
