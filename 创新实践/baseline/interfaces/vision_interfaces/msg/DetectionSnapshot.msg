# 抓拍图。与 Detection3D 配对，但走**独立话题**。
#
# 接口 03b（G2 视觉探索 -> G3 集成评测）
#
# ---------------------------------------------------------------------------
# 为什么要和 Detection3D 分开（而不是内嵌一个 sensor_msgs/Image）
#
# 一张 640x480 rgb8 是 640*480*3 = 0.92 MB。按 5 Hz、每帧 3 个目标算，
# 内嵌方案下 /detections_3d 的带宽是：
#
#     3 x 5 Hz x 0.92 MB = 13.8 MB/s ≈ 110 Mbps
#
# 一次 10 分钟实验落 rosbag 就是 **8.3 GB** —— 而接口 04 要求每次实验
# 自动落盘，磁盘目前只剩 365G。
#
# 更要紧的是传输层：RTPS 默认走 UDP，单报文约 64 KB，大消息必须分片，
# **丢一片就要重传整个 sample**。ROS2 社区公认 >=1 MB 的消息开始出问题，
# Fast DDS 在 shared memory + BEST_EFFORT 下 >0.5 MB 就有偶发丢包报告。
# 我们这条 0.92 MB/条正压在门槛上。
#
# 丢包的后果不是「图没了」，是**整条 Detection3D 丢了** —— 而它是检出率
# 指标的唯一数据来源，指标会莫名偏低且极难排查。
#
# 分开之后：轻量消息（几百字节）永远可靠，图像按需订阅。
# 数据量从约 8.3 GB/次 降到约 100 MB/次。
# ---------------------------------------------------------------------------

uint32 id                # 对应 Detection3D.id

builtin_interfaces/Time stamp   # 与对应 Detection3D.stamp **一致**（图像采集时刻）。
                                # 用 (id, stamp) 二元组做外键匹配。

sensor_msgs/CompressedImage image
# 目标 ROI 裁剪后的 JPEG，**不是整帧**。
# 典型 5-20 KB，比整帧 rgb8（0.92 MB）小两个数量级。
#
# format 字段固定为 "jpeg"。
# 裁剪范围 = 对应 Detection3D 的检测框（可含少量边距）。
# 需要整帧高清图的场景（例如报告配图）另行约定，不在此消息范围内。
